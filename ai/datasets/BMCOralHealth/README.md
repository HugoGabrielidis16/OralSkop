# BMC Oral Health 2024 (raw)

Placeholder — the raw BMC Oral Health dataset lives here locally but is **gitignored**.

Indexed-PNG semantic masks, ingested via the `mask_semantic` converter. See
`configs/data/bmc_oral_health.yaml` and `ai/DATASETS.md` for the source and class map.
