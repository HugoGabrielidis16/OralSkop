# AlphaDent (raw)

Placeholder — the raw AlphaDent dataset lives here locally but is **gitignored**.

Fetch it with:

```bash
cd ai
scripts/download_alphadent.sh
```

Native classes: `0=Abrasion, 1=Filling, 2=Crown, 3–8=Caries` (six caries subtypes).
See `configs/data/alphadent.yaml` for the class map onto the canonical taxonomy.
