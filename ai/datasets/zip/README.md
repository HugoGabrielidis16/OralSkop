# `zip/` — downloaded archives (raw)

Placeholder — downloaded dataset archives (`.zip`) are staged here before
extraction. **Gitignored**; populated by the download scripts (e.g.
`scripts/download_alphadent.sh`).
