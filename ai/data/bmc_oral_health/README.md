# bmc_oral_health (built)

Placeholder — the canonical YOLO-seg BMC Oral Health dataset is materialized here and
is **gitignored**.

Regenerate with:

```bash
cd ai
uv run python -m oralskop.data.prepare --datasets bmc_oral_health
```
