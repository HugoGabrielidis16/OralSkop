# alphadent (built)

Placeholder — the canonical YOLO-seg AlphaDent dataset is materialized here and is
**gitignored**.

Regenerate with:

```bash
cd ai
uv run python -m oralskop.data.prepare --datasets alphadent
```
