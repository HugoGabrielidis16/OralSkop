# merged (built)

Placeholder — a multi-dataset canonical YOLO-seg dataset is materialized here and is
**gitignored**.

Regenerate with (example):

```bash
cd ai
uv run python -m oralskop.data.prepare --datasets alphadent bmc_oral_health --out-name merged
```
